generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model Candle {
  id    Int    @id @default(autoincrement())
  timestamp String
  UCID Int 
  symbol String
  name String  
  price Float
  percent_change_24h Float
}
