FROM node:20-alpine
WORKDIR /app
COPY package*.json ./
RUN npm ci

# source will be mounted; keep node_modules inside container
EXPOSE 3001
CMD ["sh", "-c", "npx prisma generate && npx prisma migrate dev --name init && npm run dev"]

